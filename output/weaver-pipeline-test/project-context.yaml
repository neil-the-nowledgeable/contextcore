apiVersion: contextcore.io/v1
kind: ProjectContext
metadata:
  name: weaver-alignment
  namespace: default
spec:
  project:
    id: weaver-alignment
  targets:
  - kind: Deployment
    name: weaver-alignment
